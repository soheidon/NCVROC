% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_cv_selection.R
\name{compare_cv_selection}
\alias{compare_cv_selection}
\title{Compare model selection performance between ordinary and nested cross-validation}
\usage{
compare_cv_selection(
  data,
  outcome,
  items,
  model_sizes = NULL,
  min_items = 1,
  max_items = 4,
  item_count = NULL,
  folds = 5,
  repeats = 1,
  outer_folds = 5,
  inner_folds = 4,
  outer_repeats = 5,
  inner_repeats = 1,
  selection_metric = c("auc", "youden", "sensitivity", "specificity", "accuracy"),
  cutoff_method = c("youden", "closest_topleft"),
  sensitivity_min = NULL,
  specificity_min = NULL,
  top_n = 20,
  prefer_fewer_items = TRUE,
  stratified = TRUE,
  positive_label = 1,
  negative_label = 0,
  engine = c("Rcpp", "R"),
  parallel = FALSE,
  n_workers = NULL,
  threads_per_worker = 1L,
  seed = NULL,
  progress = interactive(),
  outer_k = NULL,
  inner_k = NULL
)
}
\arguments{
\item{data}{A data.frame containing the outcome and item columns.}

\item{outcome}{Name of the binary outcome column (bare symbol or character string).}

\item{items}{Item columns to evaluate (bare range e.g. `Q1:Q10`, bare names in `c()`,
character vector, or numeric positions).}

\item{model_sizes}{Integer vector of model sizes to evaluate (e.g. `1:5` or `c(1, 3, 5)`).
If `NULL`, resolved from `min_items:max_items` or `item_count`.}

\item{min_items}{Integer, minimum items per model (default 1, used if `model_sizes` is `NULL`).}

\item{max_items}{Integer, maximum items per model (default 4, used if `model_sizes` is `NULL`).}

\item{item_count}{String specification (e.g. `"==4"`, `"<=3"`, `"2:4"`, used if `model_sizes` is `NULL`).}

\item{folds}{Integer, number of folds for ordinary CV (default 5).}

\item{repeats}{Integer, number of independent repeats for ordinary CV (default 1).}

\item{outer_folds}{Integer, number of outer folds for nested CV (default 5). Alias: `outer_k`.}

\item{inner_folds}{Integer, number of inner folds for nested CV (default 4). Alias: `inner_k`.}

\item{outer_repeats}{Integer, number of outer repeats for nested CV (default 5).}

\item{inner_repeats}{Integer, number of inner repeats for nested CV (default 1).}

\item{selection_metric}{Metric for candidate ranking and model selection:
`"auc"` (default), `"youden"`, `"sensitivity"`, `"specificity"`, or `"accuracy"`.}

\item{cutoff_method}{Cutoff selection rule: `"youden"` (default) or `"closest_topleft"`.}

\item{sensitivity_min}{Optional minimum OOF sensitivity threshold (numeric in `[0, 1]`, default `NULL`).}

\item{specificity_min}{Optional minimum OOF specificity threshold (numeric in `[0, 1]`, default `NULL`).}

\item{top_n}{Integer, number of top candidates in ordinary ranking (default 20).}

\item{prefer_fewer_items}{Logical, prefer smaller models on ties (default `TRUE`).}

\item{stratified}{Logical, maintain class balance across folds (default `TRUE`).}

\item{positive_label}{Value indicating positive class (default 1).}

\item{negative_label}{Value indicating negative class (default 0).}

\item{engine}{Combinatorial computation engine: `"Rcpp"` (default) or `"R"`.}

\item{parallel}{Parallel mode: `FALSE` / `"none"` (default), `"outer"`, `"threads"`,
`"chunks"`, or `"hybrid"`.
- `"none"`: both ordinary and nested CV run sequentially.
- `"outer"`: nested CV uses PSOCK cluster for outer folds; ordinary CV runs sequentially.
- `"threads"`: both ordinary and nested CV use multi-threaded C++ (RcppParallel).
- `"chunks"`: both ordinary and nested CV use PSOCK cluster for candidate chunks.
- `"hybrid"`: nested CV uses outer PSOCK workers with inner threads; ordinary CV uses multi-threaded C++.}

\item{n_workers}{Integer, number of worker processes or threads (default `NULL` = auto).}

\item{threads_per_worker}{Integer, threads per PSOCK worker in `"hybrid"` mode (default 1).}

\item{seed}{Integer, random seed for reproducible fold generation.}

\item{progress}{Logical, show progress bar (default `interactive()`).}

\item{outer_k}{Alias for `outer_folds`.}

\item{inner_k}{Alias for `inner_folds`.}
}
\value{
An S3 object of class `"compare_cv_selection_result"`.
}
\description{
Compares the apparent / non-nested cross-validation performance of the single model
selected by ordinary cross-size cross-validation ([cross_size_cv()]) against the
out-of-sample generalization performance of the entire model-selection procedure
evaluated via cross-size nested cross-validation ([cross_size_nested_cv()]).
}
\details{
The difference `ordinary - nested` is returned as `selection_optimism`.


### Statistical Semantics of Model Selection Optimism
When exploring a large combinatorial candidate model space, selecting the single
best-performing model based on non-nested cross-validation leads to selection-induced
optimism.

Nested cross-validation isolates the entire model-selection process within outer training
folds and evaluates generalization performance on completely independent outer test folds.

Thus, `selection_optimism = ordinary - nested` provides an empirical estimate of the optimism
associated with model selection under the non-nested CV procedure.

Importantly, `selection_optimism` reflects optimism of the **selection procedure**,
not the bias of any specific fixed final model.

For fixed unweighted sum scores, pooled OOF AUC equals full-data apparent AUC;
thus, the ordinary AUC represents non-nested selected-model performance, while
nested AUC represents the selection procedure's out-of-sample generalization AUC.
}
