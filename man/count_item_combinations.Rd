% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_item_combinations.R
\name{count_item_combinations}
\alias{count_item_combinations}
\title{Count total item combinations}
\usage{
count_item_combinations(
  items_or_n,
  min_items = 1,
  max_items = 4,
  detail = FALSE
)
}
\arguments{
\item{items_or_n}{Character vector of item names, or a single integer n.}

\item{min_items}{Minimum items per combination (default 1).}

\item{max_items}{Maximum items per combination (default 4).}

\item{detail}{If FALSE, returns a single numeric value (total).
If TRUE, returns a data.frame with columns `n_items` and `n_combinations`.}
}
\value{
A single numeric value or a data.frame.
}
\description{
Computes the total number of k-item combinations without generating them,
using `choose(n, k)`.
}
\examples{
count_item_combinations(103, max_items = 4)
count_item_combinations(103, max_items = 4, detail = TRUE)
count_item_combinations(letters[1:5], max_items = 10)
}
