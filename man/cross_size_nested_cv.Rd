% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_size_nested_cv.R
\name{cross_size_nested_cv}
\alias{cross_size_nested_cv}
\title{Cross-size nested cross-validation for item-set model selection}
\usage{
cross_size_nested_cv(
  data,
  outcome,
  items,
  model_sizes = NULL,
  min_items = 1,
  max_items = 4,
  item_count = NULL,
  outer_folds = 5,
  inner_folds = 4,
  outer_repeats = 5,
  inner_repeats = 1,
  selection_metric = c("auc", "youden", "sensitivity", "specificity", "accuracy"),
  cutoff_method = c("youden", "closest_topleft"),
  sensitivity_min = NULL,
  specificity_min = NULL,
  prefer_fewer_items = TRUE,
  stratified = TRUE,
  positive_label = 1,
  negative_label = 0,
  engine = c("Rcpp", "R"),
  parallel = FALSE,
  n_workers = NULL,
  threads_per_worker = 1L,
  tuning = "off",
  seed = NULL,
  progress = interactive(),
  verbose = FALSE,
  outer_k = NULL,
  inner_k = NULL
)
}
\arguments{
\item{data}{A data.frame containing the outcome and item columns.}

\item{outcome}{Name of the binary outcome column (bare symbol or character string).}

\item{items}{Item columns to evaluate (bare range e.g. `Q1:Q10`, bare names in `c()`,
character vector, or numeric positions).}

\item{model_sizes}{Integer vector of model sizes to evaluate (e.g. `1:5` or `c(1, 3, 5)`).
If `NULL`, resolved from `min_items:max_items` or `item_count`.}

\item{min_items}{Integer, minimum items per model (default 1, used if `model_sizes` is `NULL`).}

\item{max_items}{Integer, maximum items per model (default 4, used if `model_sizes` is `NULL`).}

\item{item_count}{String specification (e.g. `"==4"`, `"<=3"`, `"2:4"`, used if `model_sizes` is `NULL`).}

\item{outer_folds}{Integer, number of outer CV folds (default 5). Alias: `outer_k`.}

\item{inner_folds}{Integer, number of inner CV folds (default 4). Alias: `inner_k`.}

\item{outer_repeats}{Integer, number of outer CV repeats (default 5).}

\item{inner_repeats}{Integer, number of inner CV repeats (default 1).}

\item{selection_metric}{Metric for inner model selection:
`"auc"` (default), `"youden"`, `"sensitivity"`, `"specificity"`, or `"accuracy"`.}

\item{cutoff_method}{Cutoff selection rule: `"youden"` (default) or `"closest_topleft"`.}

\item{sensitivity_min}{Optional minimum OOF sensitivity threshold (numeric in `[0, 1]`, default `NULL`).}

\item{specificity_min}{Optional minimum OOF specificity threshold (numeric in `[0, 1]`, default `NULL`).}

\item{prefer_fewer_items}{Logical, prefer smaller models on ties (default `TRUE`).}

\item{stratified}{Logical, maintain class balance across folds (default `TRUE`).}

\item{positive_label}{Value indicating positive class (default 1).}

\item{negative_label}{Value indicating negative class (default 0).}

\item{engine}{Combinatorial computation engine: `"Rcpp"` (default) or `"R"`.}

\item{parallel}{Parallel mode: `FALSE` / `"none"` (default), `"outer"`, `"threads"`,
`"chunks"`, or `"hybrid"`.
- `"none"`: sequential execution.
- `"outer"`: outer CV folds evaluated in parallel using socket workers (PSOCK cluster).
- `"threads"`: outer folds sequential; inner exhaustive candidate evaluation uses multi-threaded C++ (RcppParallel).
- `"chunks"`: outer folds sequential; inner exhaustive candidate evaluation uses socket workers.
- `"hybrid"`: outer CV folds use socket workers (`n_workers`), and each worker uses multi-threaded C++ (`threads_per_worker`).}

\item{n_workers}{Integer, number of worker processes or threads (default `NULL` = auto).}

\item{threads_per_worker}{Integer, threads per PSOCK worker in `"hybrid"` mode (default 1).}

\item{tuning}{Automatic execution-planning mode: `"off"` (default, manual
execution configuration is authoritative), `"auto"` (benchmarks legal
nested backends only when predicted serial runtime exceeds threshold), or
`"always"` (always benchmarks legal backends for non-degenerate workloads).}

\item{seed}{Integer, random seed for reproducible fold generation.}

\item{progress}{Logical, show progress bar (default `interactive()`).}

\item{verbose}{Logical, print progress messages (default `FALSE`).}

\item{outer_k}{Alias for `outer_folds`.}

\item{inner_k}{Alias for `inner_folds`.}
}
\value{
An S3 object of class `"cross_size_nested_cv_result"`. When
  \code{tuning = "auto"} or \code{"always"}, \code{settings$execution_plan}
  contains planning metadata detailing the selected execution backend and
  approximate runtime estimates. \code{tuning = "off"} attaches no execution
  plan metadata.
}
\description{
Performs nested cross-validation where the full candidate model space across
multiple model sizes is explored in each outer training set, the single best
model is selected via inner cross-validation, and the selected model is
evaluated on the completely independent outer test set.
}
