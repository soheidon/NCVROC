% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{.save_cache}
\alias{.save_cache}
\title{Save an analysis result to cache (atomic write)}
\usage{
.save_cache(
  result,
  full_results,
  cache_dir,
  cache_key,
  metadata_list,
  storage_backend
)
}
\arguments{
\item{result}{The analysis result object.}

\item{full_results}{data.frame or NULL — the full candidate table
(for single_rds backend).}

\item{cache_dir}{Character, root cache directory.}

\item{cache_key}{Character, 32-char hex key.}

\item{metadata_list}{Named list of metadata (from the calling function).}

\item{storage_backend}{Character: "memory", "single_rds", "chunked_rds", or "none".}
}
\value{
The result object (possibly modified with relative paths).
}
\description{
Writes the result to `<cache_dir>/<key>.building-<pid>/`, marks
`complete = TRUE` in metadata.rds, then atomically renames to `<key>/`.
If an existing complete `<key>/` already exists, it is first renamed to
`<key>.old-<pid>/` then removed (best-effort).
}
\keyword{internal}
