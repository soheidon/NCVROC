% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhaustive_sum_roc.R
\name{exhaustive_sum_roc}
\alias{exhaustive_sum_roc}
\title{Exhaustive ROC evaluation of all item subsets}
\usage{
exhaustive_sum_roc(
  data,
  outcome,
  items,
  min_items = 1,
  max_items = 4,
  positive_label = 1,
  negative_label = 0,
  cutoff_method = c("youden", "closest_topleft"),
  rank_by = c("auc", "youden", "sensitivity", "specificity", "accuracy"),
  top_n = NULL,
  prefer_fewer_items = TRUE,
  engine = c("R", "Rcpp"),
  progress = TRUE
)
}
\arguments{
\item{data}{A data.frame containing item columns and a binary outcome column.}

\item{outcome}{Character string naming the binary outcome column.}

\item{items}{Character vector of item column names. If NULL (default), uses
all columns except `outcome`.}

\item{min_items}{Integer, minimum number of items per combination (default 1).}

\item{max_items}{Integer, maximum number of items per combination (default 4).}

\item{positive_label}{Value in `outcome` representing a positive case (default 1).}

\item{negative_label}{Value in `outcome` representing a negative case (default 0).}

\item{cutoff_method}{Method for determining the optimal cutoff. One of
`"youden"` (maximize Youden index) or `"closest_topleft"` (minimize
Euclidean distance to (0,1) in ROC space). Default `"youden"`.}

\item{rank_by}{Metric for ranking models. One of `"auc"`, `"youden"`,
`"sensitivity"`, `"specificity"`, or `"accuracy"`. Default `"auc"`.}

\item{top_n}{Integer, return only the top N models. `NULL` returns all models.}

\item{prefer_fewer_items}{Logical. If `TRUE` and multiple models tie on
`rank_by`, models with fewer items are ranked higher. Default `TRUE`.}

\item{engine}{Character, computation engine. `"R"` (default) or `"Rcpp"`.}

\item{progress}{Logical, show progress bar? Default `TRUE`.}
}
\value{
A data.frame with columns: `rank`, `items` (comma-separated string),
  `n_items`, `auc`, `cutoff`, `sensitivity`, `specificity`, `youden`,
  `accuracy`, `ppv`, `npv`, `n_positive`, `n_negative`.
  Sorted by the chosen `rank_by` metric in descending order.
}
\description{
Enumerates all possible item combinations up to `max_items`, computes
simple sum scores for each, and evaluates predictive performance via ROC
analysis on each combination.
}
\details{
The sum score of each item combination is computed as `rowSums()`.
**Higher scores are assumed to indicate higher probability of a positive
outcome.** Users must reverse-code items beforehand if needed.

The cutoff rule is `predicted_positive = score >= cutoff`.
}
\examples{
d <- data.frame(
  y  = c(1, 1, 0, 0, 1, 0, 1, 1, 0, 0),
  q1 = c(2, 1, 2, 0, 1, 1, 2, 2, 0, 1),
  q2 = c(1, 2, 1, 1, 0, 0, 2, 1, 0, 1),
  q3 = c(2, 2, 1, 0, 1, 0, 2, 1, 1, 0)
)
exhaustive_sum_roc(d, "y", c("q1", "q2", "q3"), max_items = 2)

}
