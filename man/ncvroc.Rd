% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncvroc.R
\name{ncvroc}
\alias{ncvroc}
\title{Run a complete NCVROC analysis}
\usage{
ncvroc(
  data,
  outcome,
  items,
  min_items = 1,
  max_items = 4,
  mode = c("balanced", "quick", "thorough", "exhaustive"),
  outer_k = 5,
  inner_k = 4,
  outer_repeats = 5,
  inner_repeats = 1,
  preselect_top_n = NULL,
  preselect_by = "auc",
  selection_criterion = "auc",
  cutoff_method = "youden",
  positive_label = 1,
  negative_label = 0,
  stratified = TRUE,
  engine = "Rcpp",
  seed = NULL,
  final_search = TRUE,
  final_top_n = 20,
  final_rank_by = c("auc", "youden", "sensitivity", "specificity", "accuracy"),
  save_results = FALSE,
  output_dir = ".",
  results_storage = c("auto", "memory", "rds", "none"),
  results_name = NULL,
  results_dir = NULL,
  chunk_size = 200000L,
  cache = c("off", "reuse", "refresh"),
  cache_dir = NULL,
  progress = TRUE,
  verbose = TRUE,
  return = "full",
  item_count = NULL
)
}
\arguments{
\item{data}{A data.frame containing outcome and item columns.}

\item{outcome}{Column name (bare symbol or character string).}

\item{items}{Candidate items: bare range (`Q1:Q112`), bare names with
`c()`, character vector, existing variable, or numeric positions.}

\item{min_items}{Integer, minimum items per combination (default 1).}

\item{max_items}{Integer, maximum items per combination (default 4).}

\item{mode}{Analysis mode: `"quick"`, `"balanced"`, `"thorough"`, or
`"exhaustive"`. Default `"balanced"`.}

\item{outer_k}{Number of outer CV folds (default 5).}

\item{inner_k}{Number of inner CV folds (default 4).}

\item{outer_repeats}{Number of outer CV repeats (default 5).}

\item{inner_repeats}{Number of inner CV repeats (default 1).}

\item{preselect_top_n}{Number of top candidates to preselect, or NULL
to auto-set based on `mode`.}

\item{preselect_by}{Metric for preselection (default `"auc"`).}

\item{selection_criterion}{Metric for model selection (default `"auc"`).}

\item{cutoff_method}{Cutoff method: `"youden"` or `"closest_topleft"`.}

\item{positive_label}{Value for positive class (default 1).}

\item{negative_label}{Value for negative class (default 0).}

\item{stratified}{Logical, use stratified CV folds (default TRUE).}

\item{engine}{Computation engine: `"R"` or `"Rcpp"` (default `"Rcpp"`).}

\item{seed}{Integer seed for reproducibility (default NULL).}

\item{final_search}{Logical, run exhaustive search on full dataset
after nested CV (default TRUE).}

\item{final_top_n}{Number of top final candidates to store (`NULL` for
all, `0` for none). Default 20.}

\item{final_rank_by}{Metric for ranking final full-data candidate table:
`"auc"`, `"youden"`, `"sensitivity"`, `"specificity"`, or `"accuracy"`.}

\item{save_results}{Logical, write CSV outputs (default FALSE).}

\item{output_dir}{Directory for saved CSVs (default `"."`).}

\item{results_storage}{Where to store the full final exhaustive results:
`"auto"` (default, small→RAM, large→disk), `"memory"` (keep in RAM),
`"rds"` (save to disk), or `"none"` (discard).}

\item{results_name}{Optional label prefix for the RDS filename (only used
when results_storage saves a single RDS).}

\item{results_dir}{Directory for the full results RDS file, or NULL
(default) to use `tempdir()`. Only used for single-RDS storage.}

\item{chunk_size}{Integer, combinations per chunk (default 200000).}

\item{cache}{One of `"off"` (default), `"reuse"`, or `"refresh"`.}

\item{cache_dir}{Directory for result caching, or NULL (must be set
when `cache != "off"`).}

\item{progress}{Logical, show progress bars (default TRUE).}

\item{verbose}{Logical, print diagnostic messages (default TRUE).}

\item{return}{Return mode: `"full"` or `"summary"` (default `"full"`).}

\item{item_count}{Concise model-size specification: `"==4"` (exactly 4
items), `"<=4"` (up to 4 items), or `"2:4"` (2 through 4 items).
Cannot be combined with `min_items` or `max_items`. Default NULL.}
}
\value{
An object of class `"ncvroc_analysis"`.
}
\description{
Primary user-facing function for nested cross-validation of combinatorial
ROC-based item-set selection. Resolves outcome and item columns using
base-R style selection, prepares data, creates a configuration, runs
nested CV, and optionally performs a final exhaustive search.
}
\examples{
set.seed(42)
d <- data.frame(
  y  = sample(0:1, 60, replace = TRUE),
  Q1 = sample(0:2, 60, replace = TRUE),
  Q2 = sample(0:2, 60, replace = TRUE),
  Q3 = sample(0:2, 60, replace = TRUE)
)
\donttest{
result <- ncvroc(d, y, Q1:Q3, max_items = 2, mode = "quick",
  outer_k = 2, inner_k = 2, outer_repeats = 1, engine = "R",
  seed = 42, final_search = FALSE)
print(result)
}
}
