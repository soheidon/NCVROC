% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_sum_roc.R
\name{nested_sum_roc}
\alias{nested_sum_roc}
\title{Nested cross-validation for item-set score selection}
\usage{
nested_sum_roc(
  data,
  outcome,
  items,
  min_items = 1,
  max_items = 4,
  positive_label = 1,
  negative_label = 0,
  cutoff_method = c("youden", "closest_topleft"),
  preselect_top_n = 20,
  preselect_by = "auc",
  selection_criterion = "auc",
  outer_k = 5,
  inner_k = 4,
  outer_repeats = 1,
  inner_repeats = 1,
  stratified = TRUE,
  seed = NULL,
  engine = "R",
  progress = TRUE,
  verbose = TRUE,
  return = c("full", "summary"),
  output_dir = NULL,
  file_prefix = "NCVROC"
)
}
\arguments{
\item{data}{A data.frame containing item columns and a binary outcome column.}

\item{outcome}{Character, name of the binary outcome column.}

\item{items}{Character vector of item column names.}

\item{min_items}{Integer, minimum items per combination (default 1).}

\item{max_items}{Integer, maximum items per combination (default 4).}

\item{positive_label}{Value for a positive case (default 1).}

\item{negative_label}{Value for a negative case (default 0).}

\item{cutoff_method}{Method for the optimal cutoff: `"youden"` or
`"closest_topleft"`. Default `"youden"`.}

\item{preselect_top_n}{Integer, top N models from outer-train exhaustive
search to evaluate via inner CV (default 20).}

\item{preselect_by}{Metric for pre-selecting top candidates. One of
`"auc"`, `"youden"`, `"sensitivity"`, or `"specificity"`. Default `"auc"`.}

\item{selection_criterion}{Metric for selecting the best model via inner CV.
One of `"auc"`, `"youden"`, `"sensitivity"`, or `"specificity"`.
Default `"auc"`.}

\item{outer_k}{Integer, number of outer CV folds (default 5).}

\item{inner_k}{Integer, number of inner CV folds (default 4).}

\item{outer_repeats}{Integer, number of outer CV repeats (default 1).}

\item{inner_repeats}{Integer, number of inner CV repeats. Must be 1 in
v0.1; values > 1 warn and are reset to 1.}

\item{stratified}{Logical, use stratified folds? Must be `TRUE` in v0.1.}

\item{seed}{Integer, seed for reproducibility (default `NULL`).}

\item{engine}{Character, computation engine. Only `"R"` in v0.1.}

\item{progress}{Logical, show progress bars? Default `TRUE`.}

\item{verbose}{Logical, print progress messages? Default `TRUE`.}

\item{return}{Character, `"full"` (all details) or `"summary"` (summary
only). Only `"full"` is implemented in v0.1.}

\item{output_dir}{Character, directory for CSV output. Deferred to v0.2;
warns if non-NULL.}

\item{file_prefix}{Character, prefix for CSV filenames (default `"NCVROC"`).}
}
\value{
An object of class `"ncvroc_result"`, a list with elements:
  \item{summary}{data.frame of per-fold test performance.}
  \item{outer_results}{list of per-fold details including predictions.}
  \item{selected_models}{character vector of selected item sets per fold.}
  \item{selected_model_frequency}{data.frame of item-set selection counts.}
  \item{outer_predictions}{data.frame of all out-of-fold predictions.}
  \item{settings}{list of all argument values.}
}
\description{
Performs nested cross-validation to evaluate and select item combinations
for short screening scales. Outer cross-validation evaluates predictive
performance, while inner cross-validation selects the best item set and
cutoff within each outer training fold, reducing optimistic bias.
}
\details{
**Core assumptions:**
- Higher sum scores indicate higher probability of a positive outcome.
- Users must reverse-code items beforehand if needed.
- The cutoff rule is `predicted_positive = score >= cutoff`.
}
\examples{
\dontrun{
d <- data.frame(
  y  = sample(0:1, 100, replace = TRUE),
  q1 = sample(0:2, 100, replace = TRUE),
  q2 = sample(0:2, 100, replace = TRUE),
  q3 = sample(0:2, 100, replace = TRUE)
)
result <- nested_sum_roc(d, "y", c("q1", "q2", "q3"),
  max_items = 2, outer_k = 3, inner_k = 2, seed = 42)
summary(result)
}

}
