% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc-bruteforce.R
\name{roc_bruteforce}
\alias{roc_bruteforce}
\alias{roc_bf}
\title{Exhaustive item-combination ROC analysis without cross-validation}
\usage{
roc_bruteforce(
  data,
  outcome,
  items,
  min_items = 1,
  max_items = 4,
  cutoff_method = "youden",
  positive_label = 1,
  negative_label = 0,
  engine = c("Rcpp", "R"),
  rank_by = c("auc", "youden", "sensitivity", "specificity", "accuracy"),
  top_n = 20,
  progress = interactive(),
  save_results = FALSE,
  output_dir = ".",
  results_storage = c("rds", "memory", "none"),
  results_name = NULL,
  results_dir = NULL,
  item_count = NULL
)

roc_bf(
  data,
  outcome,
  items,
  min_items = 1,
  max_items = 4,
  cutoff_method = "youden",
  positive_label = 1,
  negative_label = 0,
  engine = c("Rcpp", "R"),
  rank_by = c("auc", "youden", "sensitivity", "specificity", "accuracy"),
  top_n = 20,
  progress = interactive(),
  save_results = FALSE,
  output_dir = ".",
  results_storage = c("rds", "memory", "none"),
  results_name = NULL,
  results_dir = NULL,
  item_count = NULL
)
}
\arguments{
\item{data}{A data.frame containing outcome and item columns.}

\item{outcome}{Column name (bare symbol or character string).}

\item{items}{Candidate items: bare range (`Q1:Q112`), bare names with
`c()`, character vector, existing variable, or numeric positions.}

\item{min_items}{Integer, minimum items per combination (default 1).}

\item{max_items}{Integer, maximum items per combination (default 4).}

\item{cutoff_method}{Cutoff method: `"youden"` or `"closest_topleft"`.}

\item{positive_label}{Value for positive class (default 1).}

\item{negative_label}{Value for negative class (default 0).}

\item{engine}{Computation engine: `"R"` or `"Rcpp"` (default `"Rcpp"`).}

\item{rank_by}{Metric for ranking: `"auc"`, `"youden"`, `"sensitivity"`,
`"specificity"`, or `"accuracy"`.}

\item{top_n}{Number of top candidates to return (NULL for all, 0 for none).}

\item{progress}{Logical, show progress bar (default `interactive()`).}

\item{save_results}{Logical, write CSV outputs (default FALSE).}

\item{output_dir}{Directory for saved CSVs (default `"."`).}

\item{results_storage}{Where to store the full results table:
`"rds"` (save to RDS file, default), `"memory"` (keep in RAM), or
`"none"` (discard).}

\item{results_name}{Optional label prefix for the RDS filename. Analysis
conditions (item count, k range, rank_by) are always appended.}

\item{results_dir}{Directory for the full results RDS file, or NULL
(default) to use a temporary directory.}

\item{item_count}{Concise model-size specification: `"==4"` (exactly 4
items), `"<=4"` (up to 4 items), or `"2:4"` (2 through 4 items).
Cannot be combined with `min_items` or `max_items`. Default NULL.}
}
\value{
An object of class `"roc_bruteforce_result"`. Use `$results`
  for the full table, `$candidates` for the `top_n` subset, and
  `$best_model` for the top-ranked model.
}
\description{
Evaluates all requested item combinations directly on the full dataset
and returns the best-performing item subsets by ROC metrics.
}
\details{
`roc_bruteforce()` reports performance on the same data used to select
items and cutoffs. These estimates are *apparent* (resubstitution)
performance and may be optimistic. Use `ncvroc()` for nested
cross-validated performance estimation.
}
\examples{
set.seed(1)
d <- data.frame(
  y  = rep(c(0, 1), each = 20),
  Q1 = rbinom(40, 2, 0.4),
  Q2 = rbinom(40, 2, 0.5),
  Q3 = rbinom(40, 2, 0.6)
)
result <- roc_bruteforce(d, y, Q1:Q3, max_items = 2, engine = "R",
  top_n = 5, progress = FALSE)
result
result$best_model
result$candidates
}
