% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ncvroc.R
\name{run_ncvroc}
\alias{run_ncvroc}
\title{Run nested CV analysis from an NCVROC configuration}
\usage{
run_ncvroc(
  data,
  items,
  config,
  seed = NULL,
  progress = TRUE,
  verbose = TRUE,
  return = c("full", "summary")
)
}
\arguments{
\item{data}{A data.frame containing outcome and item columns.}

\item{items}{Character vector of candidate item names.}

\item{config}{An `ncvroc_config` object from `ncvroc_config()`.}

\item{seed}{Integer seed for reproducibility (default NULL).}

\item{progress}{Logical, show progress bars (default TRUE).}

\item{verbose}{Logical, print diagnostic messages (default TRUE).}

\item{return}{Return mode: `"full"` or `"summary"` (default `"full"`).}
}
\value{
An `ncvroc_result` object from `nested_sum_roc()`.
}
\description{
Thin wrapper around `nested_sum_roc()` that reads all parameters from
a configuration object created by `ncvroc_config()`.
}
\examples{
cfg <- ncvroc_config("y", items = paste0("q", 1:5), max_items = 2, mode = "quick")
d <- data.frame(
  y  = sample(0:1, 100, replace = TRUE),
  q1 = sample(0:2, 100, replace = TRUE),
  q2 = sample(0:2, 100, replace = TRUE),
  q3 = sample(0:2, 100, replace = TRUE),
  q4 = sample(0:2, 100, replace = TRUE),
  q5 = sample(0:2, 100, replace = TRUE)
)
\donttest{
result <- run_ncvroc(d, paste0("q", 1:5), cfg, seed = 42)
}
}
